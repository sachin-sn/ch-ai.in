export default function BlogIndexPage() {
  return (
    <div className="mx-auto max-w-3xl px-6 py-16">
      <h1 className="font-display text-4xl font-bold text-ink">Writing</h1>
      <p className="mt-6 text-ink-dim">
        Nothing published yet — the MDX pipeline, like counter, and LinkedIn
        share button are next up on the build plan.
      </p>
    </div>
  );
}
